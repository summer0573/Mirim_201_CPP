#include <iostream>
#include <string>
#include <vector>		// vector STL »ç¿ë

using namespace std;

void main(void) {
	vector<int> v;
	int input, tot = 0;
	double avg;

	for (int i = 0; i < 5; i++) {
		cin >> input;
		v.push_back(input);
	}
}